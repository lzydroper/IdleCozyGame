# 03 — 恢复类道具批量使用（滑条 + 封顶）

**What to build:** 详情弹窗中，带属性恢复效果的道具显示「使用」按钮与数量滑条：滑条上限 = min(拥有数, 主效果属性剩余容量可支撑次数)，主效果为即时效果中的首个属性；滑条调整时显示实际生效值（含封顶，如饱食度 81/100 选 1 个 +30 口粮显示「+19（已满 100）」）；点击使用按滑条数量批量消耗并生效，弹窗停留、数量与滑条实时更新，数量归 0 时禁用；属性上限统一读取角色最大属性值（消除此前写死的 100/130 逻辑）。

**Blocked by:** 02 — 背包物品详情弹窗（基础）

**Status:** resolved

- [x] 饱食度 81/100、口粮 +30、拥有 20 时滑条上限为 1；拥有 2、饱食度 10/100 时上限为 2
- [x] 使用 1 个口粮显示「+19（已满 100）」而非名义值 +30
- [x] 批量使用消耗数量正确、属性封顶正确，无溢出浪费
- [x] 使用后弹窗停留、数量/滑条实时更新；数量归 0 使用按钮禁用
- [x] 状态层与组件测试覆盖封顶、批量、上限计算，全量测试/构建/lint 绿

## Answer

已在分支 `hero-ehco` 完成（commit `7861f1b`），全量 351 测试通过（+10）、tsc/vite build 绿、oxlint 从基线 4 错误降至 **2 错误**（7 警告不变，均为基线遗留，零新增）。

**实施要点**：
- `applySupplyItemUpdate(state, itemId, qty = 1)`：批量消耗（`useQty = min(qty, 拥有数)`），stats/pollution 按 qty 缩放并各自 clamp，属性上限改读 `player.maxFood/maxEnergy/maxSanity`（消除 `isNovaPresent ? 130 : 100` 过期硬编码）；
- `GameContext`：`useSupplyItem` 更名为 `supplyItem`（本非 hook，`use` 前缀触发 oxlint `rules-of-hooks` 误报；改名消除 3 处误报，lint 4→2 错误），并增加 `qty?` 参数；`WorkshopTab`/测试同步改名；
- `ItemDetailModal` 使用区（恢复类 = `useEffect.stats` 非空）：
  - 滑条上限 = `min(拥有数, ceil((max − 当前) / 主效果值))`——**ceil 而非 floor**：最后一个可部分生效（81/100 +30 → 上限 1，实际 +19 到满）；ADR-0016 公式已同步修正；
  - 效果预览显示**实际生效值**（含封顶）：如「饱食度 +19（已满 100）」；多效果道具（血清）显示「理智 +30｜污染 -30」，各自按 qty 缩放并 clamp；
  - 使用后弹窗停留、持有数量/滑条上限/预览实时更新（state 驱动），计数重置；qty 归 0 或属性满时滑条与按钮禁用，文案区分「物品已用完」/「属性已满，无法使用」；
- 测试：`expansion.test.tsx` 新增 3 例（批量封顶 81+60→100、批量未满 10+60→70、污染批量 clamp 50-60→0）；`ItemDetailModal.test.tsx` 新增 6 例（容量上限=1/=2、+19 已满预览、实时更新+滑条上限收窄、qty 归 0 禁用、血清多效果、非恢复类无使用区）。

