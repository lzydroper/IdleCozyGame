# 08 — 触发型被动编译与 removable Buff

**What to build:** 触发型被动在战斗初始化阶段编译为永久 Buff 并注册触发；被动不进入主动能力选择。Buff 增加可驱散标记，驱散只移除可驱散 Buff；被动生成的 Buff 默认不可驱散。

**Blocked by:** 03 — 公式与效果模板编译器；06 — Ability 执行器与主动技能

**Status:** ready-for-agent

- [ ] 触发型被动在首个回合前编译为永久 Buff，并按配置注册触发。
- [ ] 被动不参与主动能力选择。
- [ ] Buff 配置支持可驱散标记；驱散只移除可驱散 Buff。
- [ ] 被动生成的 Buff 默认不可驱散。
- [ ] 既有 Buff 触发行为不受影响；测试覆盖被动编译、触发、不可驱散与普通 Buff 可驱散。
